spring:
  datasource:
    url: ${DEV_DB_URL:${DB_URL}}
    username: ${DEV_DB_USERNAME:${DB_USERNAME}}
    password: ${DEV_DB_PASSWORD:${DB_PASSWORD}}
    driver-class-name: org.mariadb.jdbc.Driver

spero:
  security:
    jwt-url: ""
