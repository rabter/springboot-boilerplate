pluginManagement {
	repositories {
		maven { url 'https://repo.spring.io/snapshot' }
		gradlePluginPortal()
	}
}
rootProject.name = 'springboot-boilerplate'
// composite build로 연결
includeBuild('spero-commonlib')